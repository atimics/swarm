id: "006"
title: "fix(profile-page): build fails — vite not found"
priority: P2
type: bug
labels:
  - type:bug
  - priority:medium
assignees:
  - copilot-swe-agent[bot]
source: engineering-report-2026-02-15

body: |
  ## Problem

  `packages/profile-page` fails to build:

  ```
  sh: 1: vite: not found
  ```

  ## Root Cause

  `node_modules` is not installed for this workspace member. The `vite` binary is listed as a dependency but not available on PATH.

  ## Fix

  1. Ensure `pnpm install` resolves dependencies for `profile-page`
  2. Verify `vite` is in `devDependencies` (not just `dependencies`)
  3. Verify `pnpm --filter profile-page build` succeeds

  ## Acceptance Criteria

  - [ ] `pnpm --filter profile-page build` succeeds
  - [ ] `pnpm -r build` completes with no failures
