{
  "name": "@swarm/layer",
  "version": "1.0.0",
  "description": "Lambda layer with native modules (sharp) - AWS SDK is bundled with esbuild",
  "private": true,
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "scripts": {
    "build:layer": "rm -rf nodejs && mkdir -p nodejs && cp package.json nodejs/ && cd nodejs && npm install --omit=dev --platform=linux --arch=x64"
  }
}
